{
  "name": "hdr2sdr",
  "version": "1.0.0",
  "private": true,
  "description": "HDR to SDR video converter — React UI + Express/FFmpeg backend",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "start": "npm run build && npm --prefix backend start",
    "serve": "npm --prefix backend start",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
